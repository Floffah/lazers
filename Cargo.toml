[workspace]
resolver = "2"
members = [
    "boot/uefi-loader",
    "kernel/lazers-kernel",
    "libs/boot-info",
    "libs/lazers-elf",
    "user/lazers-user-echo",
]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[workspace.dependencies]
boot-info = { path = "libs/boot-info" }
lazers-elf = { path = "libs/lazers-elf" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
codegen-units = 1
lto = "thin"
